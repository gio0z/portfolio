# Lab redesign fixture

Minimal static source used by the Task 17 end-to-end publication flow test.
The bytes are uploaded through the MCP `upload_lab_asset` tool, so the
immutable artifact hash is deterministic for a fixed fixture.
