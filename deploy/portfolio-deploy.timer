[Unit]
Description=Check for portfolio deploys from GitHub main

[Timer]
# The script exits immediately when origin/main is unchanged, so a frequent
# check is cheap; it only does work when there is something to deploy.
OnCalendar=*:0/1
AccuracySec=10s
Persistent=true

[Install]
WantedBy=timers.target
